.side-bar
  .side-bar__header
    .current_user.name
      .side-bar__header-box
        .current-user-name
          てふてふ
        .icon-box
          = link_to new_group_path do
            %i.fa.fa-edit
          = link_to edit_user_path(current_user.id) do
            %i.fa.fa-cog
  .side-bar__group-list
    - current_user.groups.each do |group|
      .group-name-and-message-box
        = link_to '#' do
          %p.group-name
            = group.name
          %p.latest-message メッセージはまだありません。